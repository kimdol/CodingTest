#pragma once
#include <string>
#include <vector>
#include <iostream>

using namespace std;

namespace WallAndPoison
{
    // 백트래킹 함수
    void MaxCnt(const vector<vector<int>>& quest, int currentXp, int qCnt, vector<bool>& visits, int& maxValue)
    {
        
    }

    int solution(vector<vector<int>> quest)
    {
        int maxValue = 0;

        return maxValue;
    }
}
